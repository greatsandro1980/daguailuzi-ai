services:
  - type: web
    name: daguailuzi
    env: node
    region: singapore
    plan: free
    buildCommand: npm install && cd daguailuzi && npm install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
